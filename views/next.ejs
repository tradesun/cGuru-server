<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Your Top Priorities</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body class="page-bg">
    <!-- Fixed top menu (match index) -->
    <div class="fixed-menu-header">
      <div class="fixed-menu-header-container" style="max-width: 1390px;">
        <div class="fixed-menu-logo">
          <img src="/images/CHG_Logo_200.png" alt="Your Top Priorities" />
        </div>
        <div class="flex items-center gap-2 header-buttons">
          <button class="button-outline" onclick="history.back()" aria-label="Back to Category">Back To Category</button>
        </div>
      </div>
    </div>

    <!-- Page header (match index structure) -->
    <header class="header-bar" style="max-width: 1390px;">
      <div class="header-bar-logo">
        <img src="/images/TPOS_highres.png" alt="Your Top Priorities" />
      </div>
      <div class="header-bar-text">
        <h1 class="title-main text-2xl md:text-3xl">Your Top Priorities</h1>
        <p class="subtitle text-sm md:text-base mt-1">Based on your latest assessment, start with the top priorities and work your way down. Expand an item to see why it matters, how to do it, and download resources.</p>
      </div>
    </header>

    <!-- Main two-column layout -->
    <div class="layout-container py-6 grid grid-cols-1 lg:grid-cols-12 gap-6" style="max-width: 1400px;">
      <!-- Left: list & controls -->
      <aside class="lg:col-span-6">
        <div class="mb-3 flex items-center gap-2">
          <div class="relative flex-1">
            <input id="nextSearch" placeholder="Search actions or categories…" class="w-full rounded-xl border px-4 py-2 text-sm outline-none focus:ring" style="border-color:#E5E7EB" />
            <div class="pointer-events-none absolute right-3 top-2.5 opacity-50">🔎</div>
          </div>
        </div>

        <%- include('partials/filterBar') %>

        <ul id="actionsList" class="space-y-2"></ul>
      </aside>

      <!-- Right: details -->
      <section class="lg:col-span-6">
        <div id="detailsPanel" class="rounded-2xl border bg-white p-6 shadow-sm" style="border-color:#E5E7EB">
          <div class="text-slate-600">Select an action on the left to view details.</div>
        </div>
      </section>
    </div>

    <script src="/js/ui.js" defer></script>
    <script src="/js/next.js" defer></script>
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        try {
          if (window.FilterBarInit) { console.log('[FilterBar] init via EJS page'); window.FilterBarInit(); }
          document.dispatchEvent(new CustomEvent('filterbar:changed'));
        } catch (e) { console.error('[FilterBar] EJS init error', e); }
      });
    </script>
  </body>
</html>
